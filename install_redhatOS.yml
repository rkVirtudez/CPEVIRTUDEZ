---
 - hosts: all
   become: true
   tasks:

    - name: Installing Apache2 and PHP Packages
      package:
        name:
          - httpd
          - php
        state: latest
        update_cache: yes
      when: ansible_distribution == "RedHat"
